name: Trigger CI
on:
  pull_request:
    branches:
      - main
      - development
    # inputs:
    #   max_timeout:
    #     description: 'Maximum timeout in seconds'
    #     required: false
    #     type: number
    #     default: 300
    #   node_version:
    #     description: 'Node.js version'
    #     required: false
    #     type: string
    #     default: '24'
    #   install_command:
    #     description: 'Install command'
    #     required: false
    #     type: string
    #     default: 'npm ci'
    #   test_command:
    #     description: 'Playwright test command'
    #     required: false
    #     type: string
    #     default: 'npx playwright test'
    #   browsers:
    #     description: 'Browsers to install (comma-separated)'
    #     required: false
    #     type: string
    #     default: 'chromium,firefox,webkit'
    #   continue_on_error:
    #     description: 'Continue on test failures'
    #     required: false
    #     type: boolean
    #     default: true
    # secrets:
    #   NETLIFY_TOKEN:
    #     description: 'Netlify API token'
    #     required: true
    #   NETLIFY_SITE_ID:
    #     description: 'Netlify Site ID'
    #     required: true
    paths-ignore:
      - "**.md"
      - "docs/**"
jobs:
  setup:
    name: Run check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

  step2:
    name: Run Netlify Website Quality Checks ... But does it get executed like this?
    uses: APSPhysics/aps-github-workflows/blob/main/.github/workflows/netlify-website-quality-checks.yml@main
